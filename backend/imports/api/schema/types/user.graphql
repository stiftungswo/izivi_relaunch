type User {
  _id: ID!
  username: String!
  email: String!
  name: String!
  profile: UserProfile
  emails: [UserEmail]
  roles: [ID]
}

type UserProfile {
  firstName: String
  lastName: String
}

type UserEmail {
  address: String!
  verified: Boolean!
}
